---

all:
  children:
    server:
      hosts:
        bfivenpp:
          is_prod: true
          domain: "preprod.moi.ovh"
          syncthing_user: "ansible"
    local:
      hosts:
        localhost:
          ansible_connection: local
          syncthing_user: "bastien"
